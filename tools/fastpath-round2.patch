diff --git a/app/src/main/java/com/focusguard/admin/DeviceOwnerManager.kt b/app/src/main/java/com/focusguard/admin/DeviceOwnerManager.kt
--- a/app/src/main/java/com/focusguard/admin/DeviceOwnerManager.kt
+++ b/app/src/main/java/com/focusguard/admin/DeviceOwnerManager.kt
@@ -49,6 +49,13 @@ class DeviceOwnerManager private constructor(private val context: Context) {
         Context.MODE_PRIVATE
     )
     private var lastWebsitePolicySignature: String? = null
+    @Volatile private var deviceAdminActiveSnapshot = false
+    @Volatile private var armoredProtectionArmedSnapshot = false
+
+    init {
+        isDeviceAdminActive()
+        refreshArmoredProtectionSnapshot()
+    }
 
     companion object {
@@ -224,12 +231,20 @@ class DeviceOwnerManager private constructor(private val context: Context) {
 
     /** Check if Device Admin is active. */
     fun isDeviceAdminActive(): Boolean {
-        return try {
+        val active = try {
             dpm.isAdminActive(componentName)
         } catch (_: Exception) {
             false
         }
+        deviceAdminActiveSnapshot = active
+        return active
     }
+
+    fun isDeviceAdminActiveCached(): Boolean = deviceAdminActiveSnapshot
+
+    internal fun updateDeviceAdminActiveSnapshot(active: Boolean) {
+        deviceAdminActiveSnapshot = active
+    }
 
     fun isMaintenanceActive(): Boolean =
         isDeviceOwnerActive() && DeviceOwnerMaintenanceGate.isTemporarilyUnlocked(context)
@@ -246,6 +261,13 @@ class DeviceOwnerManager private constructor(private val context: Context) {
     fun isArmoredProtectionArmed(): Boolean =
         isBlockingProtectionArmed() || isAdultContentProtectionArmed()
+
+    fun isArmoredProtectionArmedCached(): Boolean = armoredProtectionArmedSnapshot
+
+    private fun refreshArmoredProtectionSnapshot() {
+        armoredProtectionArmedSnapshot =
+            isBlockingProtectionArmed() || isAdultContentProtectionArmed()
+    }
 
     fun protectionPhase(): ArmoredProtectionPolicy.Phase = ArmoredProtectionPolicy.phase(
         deviceOwnerActive = isDeviceOwnerActive(),
@@ -1029,6 +1051,8 @@ class DeviceOwnerManager private constructor(private val context: Context) {
                 "DeviceOwner",
                 "Não foi possível persistir o estado da proteção de bloqueio"
             )
+        } else {
+            refreshArmoredProtectionSnapshot()
         }
     }
@@ -1044,6 +1068,8 @@ class DeviceOwnerManager private constructor(private val context: Context) {
                 "DeviceOwner",
                 "Não foi possível persistir o estado da proteção de conteúdo adulto"
             )
+        } else {
+            refreshArmoredProtectionSnapshot()
         }
     }
diff --git a/app/src/main/java/com/focusguard/admin/FocusGuardDeviceAdminReceiver.kt b/app/src/main/java/com/focusguard/admin/FocusGuardDeviceAdminReceiver.kt
--- a/app/src/main/java/com/focusguard/admin/FocusGuardDeviceAdminReceiver.kt
+++ b/app/src/main/java/com/focusguard/admin/FocusGuardDeviceAdminReceiver.kt
@@ -17,11 +17,13 @@ class FocusGuardDeviceAdminReceiver : DeviceAdminReceiver() {
     override fun onEnabled(context: Context, intent: Intent) {
         super.onEnabled(context, intent)
+        DeviceOwnerManager.getInstance(context).updateDeviceAdminActiveSnapshot(true)
         DeviceAdminActivationWindow.close(context)
         Toast.makeText(context, context.getString(R.string.focusguard_device_admin_habilitado), Toast.LENGTH_SHORT).show()
     }
 
     override fun onDisabled(context: Context, intent: Intent) {
         super.onDisabled(context, intent)
+        DeviceOwnerManager.getInstance(context).updateDeviceAdminActiveSnapshot(false)
         Toast.makeText(context, context.getString(R.string.focusguard_device_admin_desabilitado), Toast.LENGTH_SHORT).show()
     }
