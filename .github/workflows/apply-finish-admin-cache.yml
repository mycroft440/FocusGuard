name: Apply Device Admin cache finish

on:
  push:
    branches: ["perf/cache-fast-path"]
    paths:
      - ".github/workflows/apply-finish-admin-cache.yml"
      - "tools/finish-admin-cache.patch"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/cache-fast-path
          fetch-depth: 0
      - name: Apply checked patch
        shell: bash
        run: |
          set -euo pipefail
          git apply --check tools/finish-admin-cache.patch
          git apply tools/finish-admin-cache.patch
          rm tools/finish-admin-cache.patch
          rm .github/workflows/apply-finish-admin-cache.yml
      - name: Commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- app/src/main/java/com/focusguard/service/BlockingAccessibilityService.kt
          git add -u -- tools/finish-admin-cache.patch .github/workflows/apply-finish-admin-cache.yml
          git commit -m "perf: remove Device Admin DPM read from accessibility hot path"
          git push origin HEAD:perf/cache-fast-path
