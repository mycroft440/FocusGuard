name: Apply HardBlock Google redirect fix

on:
  push:
    branches:
      - fix/hardblock-google-redirect

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android 16 SDK
        shell: bash
        run: |
          set -euo pipefail
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36" "build-tools;36.0.0"

      - name: Apply redirect fallback fix
        run: python3 scripts/apply_hardblock_redirect_fix.py

      - name: Compile and test redirect flow
        shell: bash
        run: |
          chmod +x gradlew
          ./gradlew :app:compileDebugKotlin \
            :app:testDebugUnitTest \
            --tests 'com.focusguard.service.ExternalRedirectNeutralizationTest' \
            --tests 'com.focusguard.accessibility.website.redirection.AddressBarRedirectionActionsPolicyTest' \
            --tests 'com.focusguard.service.BrowserInspectionCoordinatorTest' \
            --no-daemon

      - name: Commit validated fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "FocusGuard CI"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/apply-hardblock-google-redirect-fix.yml
          git rm scripts/apply_hardblock_redirect_fix.py
          git add app/src/main/java/com/focusguard/service/BlockingAccessibilityService.kt \
                  app/src/test/java/com/focusguard/service/ExternalRedirectNeutralizationTest.kt
          git commit -m "Fix HardBlock Google redirect fallback [redirect-fix-applied]"
          git push origin HEAD:fix/hardblock-google-redirect
